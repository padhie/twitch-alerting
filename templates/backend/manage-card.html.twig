<div class="card">
    <div class="card-body">
        <div class="form-group">
            {{ form_row(form.save) }}
        </div>

        <div class="form-group">
            <a href="{{ path("view", {userId: user.getId()}) }}" target="_blank" class="btn btn-primary btn-block">
                {% trans %}go_to_view{% endtrans %}
                <span class="fas fa-reply fa-rotate-180"></span>
            </a>
        </div>
    </div>
</div>
